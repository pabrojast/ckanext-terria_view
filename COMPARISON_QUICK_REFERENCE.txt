QUICK REFERENCE - Issues by Line Number
========================================

LINE(S) | ISSUE ID | SEVERITY | CATEGORY              | QUICK FIX
--------|----------|----------|----------------------|--------------------------------------------------
45      | #1       | MEDIUM   | Edge Case Handling    | Add: if not resource_id: return []
73      | #4       | MEDIUM   | Type Handling         | Check isinstance(resource_data, dict)
85-86   | #3       | MEDIUM   | Dict Access           | Use .get() instead of direct access
118-121 | #6       | CRITICAL | Initialization Order  | Initialize callbacks or add guard in get_actions()
330     | #10      | LOW      | Dead Code            | Remove or use sld_radios variable
468-469 | -        | MEDIUM   | CKAN 2.10 Compat     | Use context instead of toolkit.g
494     | #5       | MEDIUM   | JSON Error           | Wrap in try/except for JSONDecodeError
551-553 | #7       | MEDIUM   | Dict Access          | Use .get('id') for nested access
553     | #9       | MEDIUM   | Error Handling       | Add try/except around get_sld_files_from_dataset()
561     | #8       | LOW      | Type Handling        | Check if context['c'] is dict or object
586-597 | #6       | CRITICAL | Return Value         | Check None callbacks before returning


ISSUES THAT WILL CAUSE RUNTIME FAILURES
════════════════════════════════════════

1. CRITICAL (Line 586-597)
   If get_actions() is called before configure():
   Result: KeyError when CKAN tries to use None callbacks
   Symptom: Plugin actions won't work, resource_view_list won't be overridden

2. HIGH (Line 45)
   If data_dict doesn't have 'id':
   Result: TypeError when resource_show is called with None
   Symptom: 500 error when accessing resource view list for new resource

3. HIGH (Line 85-86)
   If context missing 'model' or 'session':
   Result: KeyError during auto-creation of resource view
   Symptom: 500 error, resource views not created

4. MEDIUM (Line 468-469)
   If toolkit.g.user doesn't exist (CKAN 2.10+):
   Result: AttributeError when building template variables
   Symptom: 500 error when rendering resource view

5. MEDIUM (Line 551-552)
   If package['id'] doesn't exist:
   Result: KeyError in form_template
   Symptom: Form won't render for resource view creation


DEPENDENCY VERIFICATION
═══════════════════════

All required files exist and are importable:
  ✓ config_manager.py (line 15)
  ✓ sld_processor.py (line 16)
  ✓ resource_utils.py (line 17)
  ✓ terria_config_builder.py (line 18)
  ✓ cache_manager.py (line 19)
  ✓ file_cache_manager.py (line 20)
  ✓ api_endpoints.py (line 21)
  ✓ cache_preloader.py (line 22)
  ✓ terria_json_generator.py (line 23)
  ✓ action_filters.py (line 24)

All external CKAN modules are standard:
  ✓ ckan.plugins
  ✓ ckan.plugins.toolkit
  ✓ ckan.logic.action.get


CKAN 2.10 COMPATIBILITY SCORE: 7/10
════════════════════════════════

Passing:
  ✓ IConfigurable interface (with inherit=True)
  ✓ IBlueprint interface
  ✓ IResourceView interface (with inherit=True)
  ✓ SingletonPlugin pattern
  ✓ Plugin entry point format

Needs Update:
  ⚠️ toolkit.g usage (use context-based access instead)
  ⚠️ setup.py Python 2.7 classifiers
  ⚠️ Missing CKAN version requirement specification

File size: 598 lines - Reasonable for a plugin
Complexity: Moderate - Well-organized with helper methods
Documentation: Good - Most functions have docstrings
Code Quality: Medium - Some defensive checks missing


REGRESSION TESTING CHECKLIST
═════════════════════════════

Before merging fixes, test:

□ Plugin loads successfully on startup
□ get_actions() called before configure() doesn't break
□ Resource view creation works with new resources
□ Form renders without SLD files in dataset
□ Form renders with SLD files in dataset  
□ setup_template_variables handles toolkit.g unavailable
□ JSON config parsing doesn't crash on invalid JSON
□ Context missing 'model' or 'session' doesn't crash
□ Resource object vs dict handling works both ways
□ Private resources are handled correctly
□ Cache invalidation works after CRUD operations
□ Plugin works on CKAN 2.9, 2.10, 2.11+

Critical test: Initialize plugin fresh, call get_actions() BEFORE configure()
Expected: Returns empty dict or valid callbacks (not None)
